export declare class SocketEmitter<T extends {
    [key: number]: any;
}> {
    private listeners;
    on<K extends keyof T>(event: K, listener: ((arg: T[K]) => Promise<void>) | ((arg: T[K]) => void)): void;
    removeListeners<K extends keyof T>(event: K): void;
    protected emit<K extends keyof T>(event: K, arg: T[K]): Promise<boolean>;
}
